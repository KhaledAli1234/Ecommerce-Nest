export enum OrderStatusEnum {
  Pending = 0,
  Placed = 1,
  OnWay = 2,
  Delivered = 3,
  Cancel = 4,
}

export enum OrderStatusNameEnum {
  Pending = 'Pending',
  Placed = 'Placed',
  OnWay = 'OnWay',
  Delivered = 'Delivered',
  Cancel = 'Cancel',
}

export enum PaymentEnum {
  Cash = 'Cash',
  Card = 'Card',
}
